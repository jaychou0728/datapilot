# DataPilot Phase 1 MVP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a platform where users upload Excel/CSV, view/clean data, run SQL queries (natural language or hand-written), get AI-recommended charts, and chat with AI about their data.

**Architecture:** FastAPI backend with API → Service → Data/AI layered architecture. Vue 3 frontend with a single data-detail page containing 5 tab panels. DuckDB for persistent per-dataset SQL querying. DeepSeek API for chart recommendations, SQL generation, and conversational analysis.

**Tech Stack:** Python 3.11+, FastAPI, Pandas, DuckDB, DeepSeek API, Vue 3, Element Plus, ECharts, Pinia, Axios

---

## File Structure

### Backend (`backend/`)

```
app/
├── __init__.py
├── main.py                  # FastAPI app, CORS, lifespan
├── config.py                # Settings from env vars
├── api/
│   ├── __init__.py
│   ├── router.py            # Include all sub-routers
│   ├── datasets.py          # Upload, list, get, delete, preview
│   ├── cleaning.py          # Scan, execute, undo
│   ├── query.py             # Execute SQL / natural language
│   ├── charts.py            # AI chart recommendations
│   └── chat.py              # AI conversation
├── service/
│   ├── __init__.py
│   ├── dataset_service.py
│   ├── cleaning_service.py
│   ├── query_service.py
│   ├── chart_service.py
│   └── chat_service.py
├── ai/
│   ├── __init__.py
│   └── deepseek_client.py
├── data/
│   ├── __init__.py
│   ├── duckdb_manager.py
│   └── file_store.py
├── model/
│   ├── __init__.py
│   ├── dataset.py
│   ├── cleaning.py
│   ├── query.py
│   ├── chart.py
│   └── chat.py
└── utils/
    ├── __init__.py
    ├── file_parser.py
    └── response.py
```

### Frontend (`frontend/`)

```
src/
├── main.ts
├── App.vue
├── router/index.ts
├── api/
│   ├── client.ts
│   ├── datasets.ts
│   ├── cleaning.ts
│   ├── query.ts
│   ├── charts.ts
│   └── chat.ts
├── stores/useDataset.ts
├── views/
│   ├── Dashboard.vue
│   ├── DatasetDetail.vue
│   ├── Upload.vue
│   └── History.vue
├── components/
│   ├── DataTable.vue
│   ├── ChartPanel.vue
│   ├── DatasetSidebar.vue
│   └── AiLoading.vue
└── utils/format.ts
```

---

### Task 1: Initialize Backend Project

**Files:**
- Create: `backend/requirements.txt`
- Create: `backend/app/__init__.py`

- [ ] **Step 1: Create requirements.txt**

```txt
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pandas==2.2.3
duckdb==1.1.3
openpyxl==3.1.5
python-dotenv==1.0.1
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0
```

- [ ] **Step 2: Create backend/app/__init__.py (empty)**

```bash
touch backend/app/__init__.py
```

- [ ] **Step 3: Install dependencies**

```bash
cd backend && pip install -r requirements.txt
```

- [ ] **Step 4: Commit**

```bash
git add backend/requirements.txt backend/app/__init__.py
git commit -m "chore: initialize backend project with dependencies"
```

---

### Task 2: Initialize Frontend Project

**Files:**
- Create: `frontend/` via Vite scaffold

- [ ] **Step 1: Scaffold Vue 3 + TypeScript project**

```bash
cd frontend
npm create vite@latest . -- --template vue-ts
npm install
```

- [ ] **Step 2: Install additional dependencies**

```bash
cd frontend
npm install vue-router@4 pinia axios element-plus echarts vue-echarts
npm install -D @types/node
```

- [ ] **Step 3: Commit**

```bash
git add frontend/
git commit -m "chore: initialize frontend with Vue 3 + Element Plus + ECharts"
```

---

### Task 3: Backend Config & Response Utils

**Files:**
- Create: `backend/app/config.py`
- Create: `backend/app/utils/__init__.py`
- Create: `backend/app/utils/response.py`

- [ ] **Step 1: Write config.py**

```python
import os
from dotenv import load_dotenv

load_dotenv()

DEEPSEEK_API_KEY = os.getenv("DEEPSEEK_API_KEY", "")
DEEPSEEK_BASE_URL = os.getenv("DEEPSEEK_BASE_URL", "https://api.deepseek.com")
UPLOAD_DIR = os.getenv("UPLOAD_DIR", "uploads")
DUCKDB_DIR = os.getenv("DUCKDB_DIR", "duckdb_data")
MAX_FILE_SIZE_MB = int(os.getenv("MAX_FILE_SIZE_MB", "50"))
DATA_RETENTION_DAYS = int(os.getenv("DATA_RETENTION_DAYS", "7"))
```

- [ ] **Step 2: Write utils/response.py**

```python
from typing import Any
from pydantic import BaseModel

class ApiResponse(BaseModel):
    code: int = 0
    data: Any = None
    message: str = "ok"

def success(data: Any = None, message: str = "ok") -> dict:
    return {"code": 0, "data": data, "message": message}

def error(code: int, message: str, data: Any = None) -> dict:
    return {"code": code, "data": data, "message": message}
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/config.py backend/app/utils/
git commit -m "feat: add config management and unified API response"
```

---

### Task 4: File Parser (Excel/CSV → DataFrame)

**Files:**
- Create: `backend/app/utils/file_parser.py`
- Create: `backend/tests/test_file_parser.py`

- [ ] **Step 1: Write failing test**

```python
import pytest
import pandas as pd
from pathlib import Path
from app.utils.file_parser import parse_file, ParseError

def test_parse_csv_utf8(tmp_path):
    csv_path = tmp_path / "test.csv"
    csv_path.write_text("name,age\nAlice,30\nBob,25", encoding="utf-8")
    df = parse_file(str(csv_path))
    assert list(df.columns) == ["name", "age"]
    assert len(df) == 2
    assert df["age"].dtype == "int64"

def test_parse_xlsx(tmp_path):
    xlsx_path = tmp_path / "test.xlsx"
    df_input = pd.DataFrame({"name": ["Alice"], "score": [95.5]})
    df_input.to_excel(str(xlsx_path), index=False)
    df = parse_file(str(xlsx_path))
    assert list(df.columns) == ["name", "score"]
    assert len(df) == 1

def test_parse_csv_gbk(tmp_path):
    csv_path = tmp_path / "test.csv"
    csv_path.write_text("姓名,年龄\n张三,28", encoding="gbk")
    df = parse_file(str(csv_path))
    assert "姓名" in df.columns

def test_unsupported_extension(tmp_path):
    txt_path = tmp_path / "test.txt"
    txt_path.write_text("hello")
    with pytest.raises(ParseError, match="不支持的文件格式"):
        parse_file(str(txt_path))

def test_empty_csv(tmp_path):
    csv_path = tmp_path / "empty.csv"
    csv_path.write_text("name,age\n")
    with pytest.raises(ParseError, match="文件中没有数据"):
        parse_file(str(csv_path))
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd backend && python -m pytest tests/test_file_parser.py -v
```
Expected: FAIL (module not found)

- [ ] **Step 3: Write file_parser.py**

```python
import pandas as pd
from pathlib import Path

class ParseError(Exception):
    pass

ALLOWED_EXTENSIONS = {".xlsx", ".xls", ".csv"}
ENCODINGS = ["utf-8", "gbk", "gb2312", "latin-1"]

def parse_file(file_path: str) -> pd.DataFrame:
    path = Path(file_path)
    ext = path.suffix.lower()

    if ext not in ALLOWED_EXTENSIONS:
        raise ParseError(f"不支持的文件格式: {ext}，仅支持 {', '.join(ALLOWED_EXTENSIONS)}")

    if ext == ".csv":
        df = _read_csv(file_path)
    else:
        df = pd.read_excel(file_path)

    if df.empty:
        raise ParseError("文件中没有数据")

    df.columns = [str(col).strip() for col in df.columns]
    return df

def _read_csv(file_path: str) -> pd.DataFrame:
    for enc in ENCODINGS:
        try:
            return pd.read_csv(file_path, encoding=enc)
        except (UnicodeDecodeError, UnicodeError):
            continue
    raise ParseError("无法识别文件编码，请将文件保存为 UTF-8 格式")
```

- [ ] **Step 4: Run tests**

```bash
cd backend && python -m pytest tests/test_file_parser.py -v
```
Expected: 5 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/utils/file_parser.py backend/tests/test_file_parser.py
git commit -m "feat: add file parser for CSV/Excel with encoding auto-detection"
```

---

### Task 5: DuckDB Manager

**Files:**
- Create: `backend/app/data/__init__.py`
- Create: `backend/app/data/duckdb_manager.py`
- Create: `backend/tests/test_duckdb_manager.py`

- [ ] **Step 1: Write failing test**

```python
import pytest
import pandas as pd
import os
from app.data.duckdb_manager import DuckDBManager

@pytest.fixture
def sample_df():
    return pd.DataFrame({"name": ["Alice", "Bob"], "age": [30, 25]})

def test_create_and_query(tmp_path, sample_df):
    db_path = str(tmp_path / "test.duckdb")
    mgr = DuckDBManager(db_path)
    mgr.create_table(sample_df, "users")
    result = mgr.query("SELECT * FROM users ORDER BY age")
    assert len(result) == 2
    assert result[0]["name"] == "Bob"

def test_preview_with_pagination(tmp_path, sample_df):
    db_path = str(tmp_path / "test.duckdb")
    mgr = DuckDBManager(db_path)
    mgr.create_table(sample_df, "users")
    rows, total = mgr.preview("users", page=1, page_size=1)
    assert len(rows) == 1
    assert total == 2

def test_write_operation_blocked(tmp_path, sample_df):
    db_path = str(tmp_path / "test.duckdb")
    mgr = DuckDBManager(db_path)
    mgr.create_table(sample_df, "users")
    with pytest.raises(ValueError, match="不允许写操作"):
        mgr.query("INSERT INTO users VALUES ('X', 99)")

def test_get_table_info(tmp_path, sample_df):
    db_path = str(tmp_path / "test.duckdb")
    mgr = DuckDBManager(db_path)
    mgr.create_table(sample_df, "users")
    info = mgr.get_table_info("users")
    assert len(info) == 2
    assert info[0]["column_name"] == "name"
```

- [ ] **Step 2: Run test, verify FAIL**

```bash
cd backend && python -m pytest tests/test_duckdb_manager.py -v
```
Expected: FAIL

- [ ] **Step 3: Write duckdb_manager.py**

```python
import duckdb
import pandas as pd
from typing import Any

FORBIDDEN_KEYWORDS = ["INSERT", "UPDATE", "DELETE", "DROP", "ALTER", "CREATE", "TRUNCATE"]

class DuckDBManager:
    def __init__(self, db_path: str):
        self.db_path = db_path

    def _get_connection(self) -> duckdb.DuckDBPyConnection:
        return duckdb.connect(self.db_path, read_only=False)

    def create_table(self, df: pd.DataFrame, table_name: str):
        conn = self._get_connection()
        try:
            conn.execute(f"DROP TABLE IF EXISTS {table_name}")
            conn.execute(f"CREATE TABLE {table_name} AS SELECT * FROM df")
        finally:
            conn.close()

    def overwrite_table(self, df: pd.DataFrame, table_name: str):
        self.create_table(df, table_name)

    def query(self, sql: str, limit: int = 1000) -> list[dict[str, Any]]:
        sql_upper = sql.strip().upper()
        for kw in FORBIDDEN_KEYWORDS:
            if sql_upper.startswith(kw) or f"\n{kw}" in sql_upper:
                raise ValueError(f"不允许写操作，只能执行查询。检测到: {kw}")

        conn = self._get_connection()
        try:
            safe_sql = sql.strip().rstrip(";")
            result = conn.execute(f"{safe_sql} LIMIT {limit}").fetchdf()
            return result.to_dict(orient="records")
        finally:
            conn.close()

    def preview(self, table_name: str, page: int = 1, page_size: int = 50) -> tuple[list[dict], int]:
        conn = self._get_connection()
        try:
            total = conn.execute(f"SELECT COUNT(*) FROM {table_name}").fetchone()[0]
            offset = (page - 1) * page_size
            rows = conn.execute(
                f"SELECT * FROM {table_name} LIMIT {page_size} OFFSET {offset}"
            ).fetchdf().to_dict(orient="records")
            return rows, total
        finally:
            conn.close()

    def get_table_info(self, table_name: str) -> list[dict[str, Any]]:
        conn = self._get_connection()
        try:
            df = conn.execute(f"DESCRIBE {table_name}").fetchdf()
            result = []
            for _, row in df.iterrows():
                col_name = row["column_name"]
                nulls = conn.execute(
                    f"SELECT COUNT(*) FROM {table_name} WHERE \"{col_name}\" IS NULL"
                ).fetchone()[0]
                samples = conn.execute(
                    f"SELECT DISTINCT \"{col_name}\" FROM {table_name} WHERE \"{col_name}\" IS NOT NULL LIMIT 5"
                ).fetchdf().iloc[:, 0].tolist()
                result.append({
                    "column_name": col_name,
                    "dtype": str(row["column_type"]),
                    "null_count": nulls,
                    "sample_values": samples,
                })
            return result
        finally:
            conn.close()

    def get_columns(self, table_name: str) -> list[str]:
        conn = self._get_connection()
        try:
            df = conn.execute(f"SELECT * FROM {table_name} LIMIT 0").fetchdf()
            return list(df.columns)
        finally:
            conn.close()

    def close(self):
        pass
```

- [ ] **Step 4: Run tests**

```bash
cd backend && python -m pytest tests/test_duckdb_manager.py -v
```
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/data/ backend/tests/test_duckdb_manager.py
git commit -m "feat: add DuckDB manager with safe query and pagination"
```

---

### Task 6: File Store

**Files:**
- Create: `backend/app/data/file_store.py`
- Create: `backend/tests/test_file_store.py`

- [ ] **Step 1: Write failing test**

```python
import pytest
from pathlib import Path
from app.data.file_store import FileStore

def test_save_and_get(tmp_path):
    store = FileStore(str(tmp_path))
    content = b"fake file content"
    store.save("abc-123", "sales.xlsx", content)
    saved = store.get_path("abc-123")
    assert saved.exists()
    assert (saved / "sales.xlsx").read_bytes() == content

def test_delete_dataset(tmp_path):
    store = FileStore(str(tmp_path))
    store.save("abc-123", "test.csv", b"data")
    store.delete("abc-123")
    assert not store.get_path("abc-123").exists()

def test_cleanup_old_files(tmp_path):
    import time, os
    store = FileStore(str(tmp_path))
    store.save("old-ds", "f.csv", b"x")
    old_dir = store.get_path("old-ds")
    # Mock old access time
    os.utime(old_dir, (time.time() - 8*86400, time.time() - 8*86400))
    store.cleanup(retention_days=7)
    assert not old_dir.exists()
```

- [ ] **Step 2: Run test, verify FAIL**

```bash
cd backend && python -m pytest tests/test_file_store.py -v
```
Expected: FAIL

- [ ] **Step 3: Write file_store.py**

```python
import shutil
import time
import os
from pathlib import Path

class FileStore:
    def __init__(self, base_dir: str):
        self.base_dir = Path(base_dir)
        self.base_dir.mkdir(parents=True, exist_ok=True)

    def save(self, dataset_id: str, filename: str, content: bytes) -> Path:
        ds_dir = self.base_dir / dataset_id
        ds_dir.mkdir(parents=True, exist_ok=True)
        file_path = ds_dir / filename
        file_path.write_bytes(content)
        return ds_dir

    def get_path(self, dataset_id: str) -> Path:
        return self.base_dir / dataset_id

    def delete(self, dataset_id: str):
        ds_dir = self.base_dir / dataset_id
        if ds_dir.exists():
            shutil.rmtree(ds_dir)

    def cleanup(self, retention_days: int = 7):
        now = time.time()
        cutoff = now - retention_days * 86400
        for ds_dir in self.base_dir.iterdir():
            if ds_dir.is_dir():
                mtime = os.path.getmtime(ds_dir)
                if mtime < cutoff:
                    shutil.rmtree(ds_dir)
```

- [ ] **Step 4: Run tests**

```bash
cd backend && python -m pytest tests/test_file_store.py -v
```
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/data/file_store.py backend/tests/test_file_store.py
git commit -m "feat: add file store with cleanup for old datasets"
```

---

### Task 7: Dataset Models

**Files:**
- Create: `backend/app/model/__init__.py`
- Create: `backend/app/model/dataset.py`

- [ ] **Step 1: Write dataset.py**

```python
from pydantic import BaseModel
from datetime import datetime

class ColumnInfo(BaseModel):
    name: str
    dtype: str
    null_count: int
    sample_values: list

class DatasetInfo(BaseModel):
    id: str
    name: str
    rows: int
    columns: int
    fields: list[ColumnInfo]
    created_at: datetime

class PreviewResponse(BaseModel):
    columns: list[str]
    rows: list[dict]
    total_rows: int
    page: int
    page_size: int
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/model/
git commit -m "feat: add dataset Pydantic models"
```

---

### Task 8: Dataset Service

**Files:**
- Create: `backend/app/service/__init__.py`
- Create: `backend/app/service/dataset_service.py`
- Create: `backend/tests/test_dataset_service.py`

- [ ] **Step 1: Write failing test**

```python
import pytest
import pandas as pd
from pathlib import Path
from app.service.dataset_service import DatasetService
from app.data.file_store import FileStore
from app.model.dataset import DatasetInfo

@pytest.fixture
def service(tmp_path):
    file_store = FileStore(str(tmp_path / "uploads"))
    db_dir = str(tmp_path / "duckdb")
    return DatasetService(file_store=file_store, duckdb_dir=db_dir)

def test_ingest_csv(service, tmp_path):
    csv_path = tmp_path / "test.csv"
    csv_path.write_text("name,age\nAlice,30\nBob,25", encoding="utf-8")
    content = csv_path.read_bytes()
    info = service.ingest("test.csv", content)
    assert info.name == "test.csv"
    assert info.rows == 2
    assert info.columns == 2
    assert len(info.fields) == 2

def test_preview(service, tmp_path):
    csv_path = tmp_path / "data.csv"
    csv_path.write_text("x,y\n1,2\n3,4\n5,6", encoding="utf-8")
    content = csv_path.read_bytes()
    info = service.ingest("data.csv", content)
    preview = service.preview(info.id, page=1, page_size=2)
    assert preview["total_rows"] == 3
    assert len(preview["rows"]) == 2

def test_list_datasets(service, tmp_path):
    csv_path = tmp_path / "a.csv"
    csv_path.write_text("x\n1", encoding="utf-8")
    content = csv_path.read_bytes()
    service.ingest("a.csv", content)
    datasets = service.list_all()
    assert len(datasets) == 1

def test_delete_dataset(service, tmp_path):
    csv_path = tmp_path / "x.csv"
    csv_path.write_text("a\n1", encoding="utf-8")
    content = csv_path.read_bytes()
    info = service.ingest("x.csv", content)
    service.delete(info.id)
    assert len(service.list_all()) == 0
```

- [ ] **Step 2: Run test, verify FAIL**

```bash
cd backend && python -m pytest tests/test_dataset_service.py -v
```
Expected: FAIL

- [ ] **Step 3: Write dataset_service.py**

```python
import uuid
import os
import time
from datetime import datetime
from typing import Any
import pandas as pd
from app.utils.file_parser import parse_file, ParseError
from app.data.file_store import FileStore
from app.data.duckdb_manager import DuckDBManager
from app.model.dataset import DatasetInfo, ColumnInfo, PreviewResponse

class DatasetService:
    def __init__(self, file_store: FileStore, duckdb_dir: str):
        self.file_store = file_store
        self.duckdb_dir = duckdb_dir
        os.makedirs(duckdb_dir, exist_ok=True)

    def ingest(self, filename: str, content: bytes) -> DatasetInfo:
        dataset_id = str(uuid.uuid4())
        self.file_store.save(dataset_id, filename, content)
        file_path = self.file_store.get_path(dataset_id) / filename
        df = parse_file(str(file_path))
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        mgr = DuckDBManager(db_path)
        mgr.create_table(df, "data")
        fields = self._build_fields(df, mgr)
        return DatasetInfo(
            id=dataset_id,
            name=filename,
            rows=len(df),
            columns=len(df.columns),
            fields=fields,
            created_at=datetime.now(),
        )

    def _build_fields(self, df: pd.DataFrame, mgr: DuckDBManager) -> list[ColumnInfo]:
        info = mgr.get_table_info("data")
        result = []
        for i, col_info in enumerate(info):
            result.append(ColumnInfo(
                name=col_info["column_name"],
                dtype=col_info["dtype"],
                null_count=col_info["null_count"],
                sample_values=col_info["sample_values"],
            ))
        return result

    def preview(self, dataset_id: str, page: int = 1, page_size: int = 50) -> dict:
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        mgr = DuckDBManager(db_path)
        columns = mgr.get_columns("data")
        rows, total = mgr.preview("data", page, page_size)
        return {"columns": columns, "rows": rows, "total_rows": total, "page": page, "page_size": page_size}

    def get_info(self, dataset_id: str) -> dict | None:
        ds_dir = self.file_store.get_path(dataset_id)
        if not ds_dir.exists():
            return None
        files = list(ds_dir.iterdir())
        if not files:
            return None
        filename = files[0].name
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        if not os.path.exists(db_path):
            return None
        mgr = DuckDBManager(db_path)
        columns = mgr.get_columns("data")
        rows_count = mgr.query("SELECT COUNT(*) AS cnt FROM data")[0]["cnt"]
        fields = mgr.get_table_info("data")
        return {
            "id": dataset_id, "name": filename, "rows": rows_count,
            "columns": len(columns), "fields": fields,
        }

    def list_all(self) -> list[dict]:
        datasets = []
        for ds_dir in self.file_store.base_dir.iterdir():
            if ds_dir.is_dir():
                info = self.get_info(ds_dir.name)
                if info:
                    datasets.append(info)
        datasets.sort(key=lambda d: d.get("id", ""), reverse=True)
        return datasets

    def delete(self, dataset_id: str):
        self.file_store.delete(dataset_id)
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        if os.path.exists(db_path):
            os.remove(db_path)

    def get_table_name(self, dataset_id: str) -> str:
        return "data"

    def get_db_path(self, dataset_id: str) -> str:
        return os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
```

- [ ] **Step 4: Run tests**

```bash
cd backend && python -m pytest tests/test_dataset_service.py -v
```
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/service/ backend/tests/test_dataset_service.py
git commit -m "feat: add dataset service with ingest, preview, list, delete"
```

---

### Task 9: Cleaning Models

**Files:**
- Create: `backend/app/model/cleaning.py`

- [ ] **Step 1: Write cleaning.py**

```python
from pydantic import BaseModel

class CleaningIssue(BaseModel):
    type: str  # "duplicate" | "null" | "whitespace" | "type_mismatch" | "outlier"
    column: str | None = None
    count: int
    sample: list = []
    severity: str = "medium"  # "high" | "medium" | "low"

class ScanResponse(BaseModel):
    issues: list[CleaningIssue]
    summary: str

class CleanRequest(BaseModel):
    dataset_id: str
    actions: list[str]  # ["deduplicate", "fill_null", "trim_whitespace", "fix_types", "remove_outliers"]
    fill_strategy: str = "mean"

class CleanResult(BaseModel):
    rows_before: int
    rows_after: int
    changes: list[str]
    can_undo: bool = True
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/model/cleaning.py
git commit -m "feat: add cleaning Pydantic models"
```

---

### Task 10: Cleaning Service

**Files:**
- Create: `backend/app/service/cleaning_service.py`
- Create: `backend/tests/test_cleaning_service.py`

- [ ] **Step 1: Write failing test**

```python
import pytest
import os
import pandas as pd
from app.data.duckdb_manager import DuckDBManager
from app.service.cleaning_service import CleaningService

@pytest.fixture
def sample_df():
    return pd.DataFrame({
        "name": ["Alice", "Bob ", "Alice", "", "Eve"],
        "age": [30, 25, 30, None, 99],
        "date": ["2024-01-01", "2024/02/01", "2024-01-01", "2024-03-01", "not_a_date"],
    })

@pytest.fixture
def service(tmp_path, sample_df):
    db_path = str(tmp_path / "test.duckdb")
    mgr = DuckDBManager(db_path)
    mgr.create_table(sample_df, "data")
    return CleaningService(db_path=db_path, table_name="data")

def test_scan_finds_duplicates(service):
    issues = service.scan()
    dup_issue = next((i for i in issues if i.type == "duplicate"), None)
    assert dup_issue is not None
    assert dup_issue.count > 0

def test_scan_finds_nulls(service):
    issues = service.scan()
    null_issue = next((i for i in issues if i.type == "null"), None)
    assert null_issue is not None

def test_execute_deduplicate(service):
    before = len(service._query("SELECT * FROM data"))
    result = service.execute(["deduplicate"], "mean")
    after = len(service._query("SELECT * FROM data"))
    assert after < before
    assert "去重" in str(result.changes)

def test_undo(service):
    before = len(service._query("SELECT * FROM data"))
    service.execute(["deduplicate"], "mean")
    service.undo()
    after_undo = len(service._query("SELECT * FROM data"))
    assert after_undo == before
```

- [ ] **Step 2: Run test, verify FAIL**

```bash
cd backend && python -m pytest tests/test_cleaning_service.py -v
```
Expected: FAIL

- [ ] **Step 3: Write cleaning_service.py**

```python
import pandas as pd
import numpy as np
from app.data.duckdb_manager import DuckDBManager
from app.model.cleaning import CleaningIssue, CleanResult

class CleaningService:
    def __init__(self, db_path: str, table_name: str):
        self.db_path = db_path
        self.table_name = table_name
        self._backup_df: pd.DataFrame | None = None

    def _get_df(self) -> pd.DataFrame:
        mgr = DuckDBManager(self.db_path)
        return pd.DataFrame(mgr.query(f"SELECT * FROM {self.table_name}", limit=1000000))

    def _query(self, sql: str) -> list[dict]:
        mgr = DuckDBManager(self.db_path)
        return mgr.query(sql, limit=1000000)

    def scan(self) -> list[CleaningIssue]:
        df = self._get_df()
        issues = []

        # Duplicates
        dup_count = df.duplicated().sum()
        if dup_count > 0:
            dup_samples = df[df.duplicated(keep=False)].head(3).to_dict(orient="records")
            issues.append(CleaningIssue(
                type="duplicate", count=int(dup_count),
                sample=dup_samples, severity="high"
            ))

        # Nulls per column
        for col in df.columns:
            null_count = df[col].isnull().sum()
            if null_count > 0:
                issues.append(CleaningIssue(
                    type="null", column=col, count=int(null_count),
                    severity="high" if null_count / len(df) > 0.3 else "medium"
                ))

        # Whitespace in string columns
        for col in df.select_dtypes(include=["object"]).columns:
            mask = df[col].astype(str).str.strip() != df[col].astype(str)
            ws_count = mask.sum()
            if ws_count > 0:
                bad = df[mask].head(3)[col].tolist()
                issues.append(CleaningIssue(
                    type="whitespace", column=col, count=int(ws_count),
                    sample=bad, severity="low"
                ))

        # Type mismatches (try numeric conversion on object columns)
        for col in df.select_dtypes(include=["object"]).columns:
            non_null = df[col].dropna()
            if len(non_null) == 0:
                issues.append(CleaningIssue(
                    type="null", column=col, count=len(df),
                    sample=[], severity="high"
                ))
                continue
            converted = pd.to_numeric(non_null, errors="coerce")
            fail_count = converted.isnull().sum()
            if 0 < fail_count < len(non_null):
                bad = non_null[converted.isnull()].head(3).tolist()
                issues.append(CleaningIssue(
                    type="type_mismatch", column=col, count=int(fail_count),
                    sample=bad, severity="medium"
                ))

        # Outliers (IQR on numeric columns)
        for col in df.select_dtypes(include=[np.number]).columns:
            q1 = df[col].quantile(0.25)
            q3 = df[col].quantile(0.75)
            iqr = q3 - q1
            lower = q1 - 1.5 * iqr
            upper = q3 + 1.5 * iqr
            outlier_mask = (df[col] < lower) | (df[col] > upper)
            if outlier_mask.sum() > 0:
                bad = df[outlier_mask].head(3)[col].tolist()
                issues.append(CleaningIssue(
                    type="outlier", column=col, count=int(outlier_mask.sum()),
                    sample=bad, severity="low"
                ))

        return issues

    def execute(self, actions: list[str], fill_strategy: str = "mean") -> CleanResult:
        df = self._get_df()
        self._backup_df = df.copy()
        rows_before = len(df)
        changes = []

        if "deduplicate" in actions:
            n = df.duplicated().sum()
            if n > 0:
                df = df.drop_duplicates()
                changes.append(f"删除重复行 {n} 条")

        if "trim_whitespace" in actions:
            for col in df.select_dtypes(include=["object"]).columns:
                df[col] = df[col].astype(str).str.strip()
            changes.append("去除文本前后空格")

        if "fill_null" in actions:
            filled_cols = []
            for col in df.columns:
                if df[col].isnull().sum() > 0:
                    if fill_strategy == "drop":
                        df = df.dropna(subset=[col])
                        filled_cols.append(f"删除列'{col}'空值所在行")
                    elif pd.api.types.is_numeric_dtype(df[col]):
                        val = df[col].mean() if fill_strategy == "mean" else df[col].median()
                        df[col] = df[col].fillna(val)
                        filled_cols.append(f"用{fill_strategy}填充列'{col}'")
                    else:
                        mode_val = df[col].mode()
                        fill_val = mode_val[0] if len(mode_val) > 0 else "未知"
                        df[col] = df[col].fillna(fill_val)
                        filled_cols.append(f"用众数填充列'{col}'")
            changes.extend(filled_cols)

        if "fix_types" in actions:
            for col in df.select_dtypes(include=["object"]).columns:
                try:
                    df[col] = pd.to_datetime(df[col])
                    changes.append(f"列'{col}'转换为日期类型")
                except (ValueError, TypeError):
                    converted = pd.to_numeric(df[col], errors="coerce")
                    if converted.notna().sum() > len(df) * 0.8:
                        df[col] = converted
                        changes.append(f"列'{col}'转换为数字类型")

        if "remove_outliers" in actions:
            for col in df.select_dtypes(include=[np.number]).columns:
                q1, q3 = df[col].quantile(0.25), df[col].quantile(0.75)
                iqr = q3 - q1
                lower, upper = q1 - 1.5 * iqr, q3 + 1.5 * iqr
                n = ((df[col] < lower) | (df[col] > upper)).sum()
                if n > 0:
                    df = df[(df[col] >= lower) & (df[col] <= upper)]
                    changes.append(f"移除列'{col}'异常值 {n} 条")

        mgr = DuckDBManager(self.db_path)
        mgr.overwrite_table(df, self.table_name)
        return CleanResult(
            rows_before=rows_before,
            rows_after=len(df),
            changes=changes,
            can_undo=True,
        )

    def undo(self) -> bool:
        if self._backup_df is None:
            return False
        mgr = DuckDBManager(self.db_path)
        mgr.overwrite_table(self._backup_df, self.table_name)
        self._backup_df = None
        return True
```

- [ ] **Step 4: Run tests**

```bash
cd backend && python -m pytest tests/test_cleaning_service.py -v
```
Expected: 4 PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/service/cleaning_service.py backend/tests/test_cleaning_service.py
git commit -m "feat: add cleaning service with scan, execute, and undo"
```

---

### Task 11: DeepSeek Client

**Files:**
- Create: `backend/app/ai/__init__.py`
- Create: `backend/app/ai/deepseek_client.py`

- [ ] **Step 1: Write deepseek_client.py**

```python
import json
import httpx
from app.config import DEEPSEEK_API_KEY, DEEPSEEK_BASE_URL

class DeepSeekClient:
    def __init__(self):
        self.api_key = DEEPSEEK_API_KEY
        self.base_url = DEEPSEEK_BASE_URL
        self.timeout = 30.0

    def _is_configured(self) -> bool:
        return bool(self.api_key)

    async def chat(self, messages: list[dict], temperature: float = 0.7) -> str:
        if not self._is_configured():
            return "AI 服务未配置，请在 .env 中设置 DEEPSEEK_API_KEY"

        headers = {
            "Authorization": f"Bearer {self.api_key}",
            "Content-Type": "application/json",
        }
        payload = {
            "model": "deepseek-chat",
            "messages": messages,
            "temperature": temperature,
            "max_tokens": 4096,
        }
        try:
            async with httpx.AsyncClient(timeout=self.timeout) as client:
                resp = await client.post(
                    f"{self.base_url}/v1/chat/completions",
                    headers=headers, json=payload,
                )
                if resp.status_code == 402:
                    return "AI 服务账户余额不足"
                if resp.status_code == 429:
                    return "请求过于频繁，请稍后再试"
                resp.raise_for_status()
                data = resp.json()
                return data["choices"][0]["message"]["content"]
        except httpx.TimeoutException:
            return "AI 服务响应超时，请重试"
        except httpx.HTTPError as e:
            return f"AI 服务异常: {e}"

    async def generate_sql(self, table_schema: str, question: str) -> str | None:
        if not self._is_configured():
            return None

        prompt = f"""你是一个 SQL 专家。根据以下表结构生成 DuckDB SQL 查询。

表名: data
表结构:
{table_schema}

用户问题: {question}

要求:
- 只返回 SQL 语句，不要任何解释
- 只能生成 SELECT 语句
- 如果用户的问题无法用 SQL 回答，返回: CANNOT_GENERATE

SQL:"""
        response = await self.chat([
            {"role": "system", "content": "你是一个只输出 SQL 语句的助手。"},
            {"role": "user", "content": prompt},
        ], temperature=0.1)

        sql = response.strip()
        if sql.startswith("```sql"):
            sql = sql.split("```sql")[1].split("```")[0].strip()
        elif sql.startswith("```"):
            sql = sql.split("```")[1].split("```")[0].strip()

        if "CANNOT_GENERATE" in sql:
            return None
        sql_upper = sql.upper()
        forbidden = ["INSERT", "UPDATE", "DELETE", "DROP", "ALTER", "CREATE", "TRUNCATE"]
        for kw in forbidden:
            if kw in sql_upper:
                return None
        return sql

    async def explain_sql(self, sql: str) -> str:
        if not self._is_configured():
            return ""
        prompt = f"用一句话中文解释这条 SQL 查询的含义:\n{sql}"
        return await self.chat([
            {"role": "user", "content": prompt},
        ], temperature=0.3)

    async def explain_query_result(self, sql: str, result_summary: str, question: str) -> str:
        if not self._is_configured():
            return ""
        prompt = f"""用户问题: {question}
执行的 SQL: {sql}
查询结果摘要: {result_summary}

用 1-3 句话中文解读这个查询结果。"""
        return await self.chat([
            {"role": "user", "content": prompt},
        ], temperature=0.5)

    async def recommend_charts(self, table_schema: str, sample_rows: str, hint: str = "") -> list[dict]:
        if not self._is_configured():
            return []

        prompt = f"""根据以下数据结构推荐 2-3 个最合适的图表。

表结构:
{table_schema}

前 10 行数据:
{sample_rows}

用户偏好: {hint or "自动推荐"}

返回 JSON 数组（只返回 JSON，不要其他内容）：
[
  {{
    "title": "图表标题",
    "chart_type": "bar|line|pie|scatter",
    "reason": "推荐理由",
    "echarts_option": {{ ... 完整的 ECharts 配置对象，包含 xAxis、yAxis、series 等 }}
  }}
]

要求:
- echarts_option 必须是完整可用的 ECharts 配置
- series 中的 data 使用实际数据的前若干条
- 图表类型要与数据特征匹配
- 用中文命名 title 和 axis 标签"""

        response = await self.chat([
            {"role": "system", "content": "你是一个只输出 JSON 的数据可视化专家。"},
            {"role": "user", "content": prompt},
        ], temperature=0.3)

        text = response.strip()
        if text.startswith("```json"):
            text = text.split("```json")[1].split("```")[0].strip()
        elif text.startswith("```"):
            text = text.split("```")[1].split("```")[0].strip()
        try:
            charts = json.loads(text)
            return charts if isinstance(charts, list) else []
        except json.JSONDecodeError:
            return []

    async def chat_about_data(
        self, table_schema: str, user_message: str, history: list[dict] | None = None
    ) -> str:
        if not self._is_configured():
            return "AI 服务未配置"

        system_prompt = f"""你是一个数据分析助手。用户正在分析一份数据。

数据表名: data
表结构:
{table_schema}

你可以:
1. 回答关于数据的问题
2. 建议生成 SQL 查询来获取答案（在回复中用 ```sql ... ``` 格式）
3. 给出数据分析建议

用中文回复，简洁专业。"""

        messages = [{"role": "system", "content": system_prompt}]
        if history:
            messages.extend(history)
        messages.append({"role": "user", "content": user_message})

        return await self.chat(messages, temperature=0.7)
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/ai/
git commit -m "feat: add DeepSeek client for chat, SQL generation, and chart recommendations"
```

---

### Task 12: Query Service

**Files:**
- Create: `backend/app/model/query.py`
- Create: `backend/app/service/query_service.py`
- Create: `backend/tests/test_query_service.py`

- [ ] **Step 1: Write query model**

```python
from pydantic import BaseModel

class QueryRequest(BaseModel):
    dataset_id: str
    sql: str | None = None
    natural_language: str | None = None

class QueryResult(BaseModel):
    columns: list[str]
    rows: list[dict]
    row_count: int
    executed_sql: str
    ai_explanation: str | None = None
```

- [ ] **Step 2: Write failing test**

```python
import pytest
import os
import pandas as pd
from app.data.duckdb_manager import DuckDBManager
from app.data.file_store import FileStore
from app.service.dataset_service import DatasetService
from app.service.query_service import QueryService

@pytest.fixture
def svc(tmp_path):
    fs = FileStore(str(tmp_path / "uploads"))
    db_dir = str(tmp_path / "duckdb")
    ds = DatasetService(fs, db_dir)
    csv = tmp_path / "test.csv"
    csv.write_text("name,age\nAlice,30\nBob,25", encoding="utf-8")
    info = ds.ingest("test.csv", csv.read_bytes())
    return QueryService(db_dir), info.id

def test_execute_sql(svc):
    qs, ds_id = svc
    result = qs.execute(ds_id, sql="SELECT * FROM data WHERE age > 26")
    assert result.row_count == 1
    assert result.rows[0]["name"] == "Alice"

def test_sql_with_forbidden_keyword(svc):
    qs, ds_id = svc
    with pytest.raises(ValueError):
        qs.execute(ds_id, sql="DROP TABLE data")
```

- [ ] **Step 3: Write query_service.py**

```python
import os
import json
from app.data.duckdb_manager import DuckDBManager
from app.ai.deepseek_client import DeepSeekClient
from app.model.query import QueryResult

class QueryService:
    def __init__(self, duckdb_dir: str):
        self.duckdb_dir = duckdb_dir
        self.ai = DeepSeekClient()

    def execute(self, dataset_id: str, sql: str | None = None,
                natural_language: str | None = None) -> QueryResult:
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        if not os.path.exists(db_path):
            raise FileNotFoundError(f"数据集 {dataset_id} 不存在")

        mgr = DuckDBManager(db_path)

        if natural_language and not sql:
            table_info = mgr.get_table_info("data")
            schema = json.dumps(table_info, ensure_ascii=False)
            import asyncio
            sql = asyncio.get_event_loop().run_until_complete(
                self.ai.generate_sql(schema, natural_language)
            )
            if sql is None:
                raise ValueError("AI 无法理解您的问题，请换个方式描述或直接输入 SQL")

        if not sql:
            raise ValueError("请提供 SQL 或自然语言描述")

        rows = mgr.query(sql)
        columns = mgr.get_columns("data")
        if rows:
            columns = list(rows[0].keys())

        explanation = None
        if natural_language:
            import asyncio
            summary = f"{len(rows)} 行, 列: {', '.join(columns)}"
            explanation = asyncio.get_event_loop().run_until_complete(
                self.ai.explain_query_result(sql, summary, natural_language)
            )

        return QueryResult(
            columns=columns, rows=rows, row_count=len(rows),
            executed_sql=sql, ai_explanation=explanation,
        )
```

- [ ] **Step 4: Run tests**

```bash
cd backend && python -m pytest tests/test_query_service.py -v
```
Expected: 2 PASS (non-AI tests pass; AI tests depend on API key)

- [ ] **Step 5: Commit**

```bash
git add backend/app/model/query.py backend/app/service/query_service.py backend/tests/test_query_service.py
git commit -m "feat: add query service with SQL execution and AI generation fallback"
```

---

### Task 13: Chart & Chat Services

**Files:**
- Create: `backend/app/model/chart.py`
- Create: `backend/app/model/chat.py`
- Create: `backend/app/service/chart_service.py`
- Create: `backend/app/service/chat_service.py`

- [ ] **Step 1: Write chart model**

```python
from pydantic import BaseModel

class ChartRecommendRequest(BaseModel):
    dataset_id: str
    hint: str | None = None

class ChartItem(BaseModel):
    title: str
    chart_type: str
    echarts_option: dict
    reason: str

class ChartRecommendResponse(BaseModel):
    charts: list[ChartItem]
```

- [ ] **Step 2: Write chat model**

```python
from pydantic import BaseModel

class ChatRequest(BaseModel):
    dataset_id: str
    message: str
    history: list[dict] | None = None

class ChatResponse(BaseModel):
    answer: str
    sql_generated: str | None = None
    chart_suggestion: dict | None = None
```

- [ ] **Step 3: Write chart_service.py**

```python
import os
import json
import asyncio
from app.data.duckdb_manager import DuckDBManager
from app.ai.deepseek_client import DeepSeekClient
from app.model.chart import ChartItem

class ChartService:
    def __init__(self, duckdb_dir: str):
        self.duckdb_dir = duckdb_dir
        self.ai = DeepSeekClient()

    async def recommend(self, dataset_id: str, hint: str | None = None) -> list[ChartItem]:
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        mgr = DuckDBManager(db_path)
        table_info = mgr.get_table_info("data")
        schema = json.dumps(table_info, ensure_ascii=False)

        sample_rows = mgr.query("SELECT * FROM data LIMIT 10")
        sample_str = json.dumps(sample_rows, ensure_ascii=False)

        charts = await self.ai.recommend_charts(schema, sample_str, hint or "")
        return [ChartItem(**c) for c in charts]
```

- [ ] **Step 4: Write chat_service.py**

```python
import os
import json
import asyncio
from app.data.duckdb_manager import DuckDBManager
from app.ai.deepseek_client import DeepSeekClient
from app.model.chat import ChatResponse

class ChatService:
    def __init__(self, duckdb_dir: str):
        self.duckdb_dir = duckdb_dir
        self.ai = DeepSeekClient()

    async def chat(self, dataset_id: str, message: str,
                   history: list[dict] | None = None) -> ChatResponse:
        db_path = os.path.join(self.duckdb_dir, f"{dataset_id}.duckdb")
        mgr = DuckDBManager(db_path)
        table_info = mgr.get_table_info("data")
        schema = json.dumps(table_info, ensure_ascii=False)

        answer = await self.ai.chat_about_data(schema, message, history)

        # Detect SQL block in response
        sql_generated = None
        if "```sql" in answer:
            parts = answer.split("```sql")
            if len(parts) > 1:
                sql_block = parts[1].split("```")[0].strip()
                sql_generated = sql_block

        return ChatResponse(
            answer=answer,
            sql_generated=sql_generated,
        )
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/model/chart.py backend/app/model/chat.py backend/app/service/chart_service.py backend/app/service/chat_service.py
git commit -m "feat: add chart recommendation and chat services"
```

---

### Task 14: Backend API Routes — Datasets

**Files:**
- Create: `backend/app/api/__init__.py`
- Create: `backend/app/api/datasets.py`

- [ ] **Step 1: Write datasets API**

```python
from fastapi import APIRouter, UploadFile, File, HTTPException, Query
from app.service.dataset_service import DatasetService
from app.data.file_store import FileStore
from app.config import UPLOAD_DIR, DUCKDB_DIR, MAX_FILE_SIZE_MB
from app.utils.response import success, error

router = APIRouter(prefix="/api/datasets", tags=["datasets"])

file_store = FileStore(UPLOAD_DIR)


def get_service():
    return DatasetService(file_store=file_store, duckdb_dir=DUCKDB_DIR)


@router.post("/upload")
async def upload(file: UploadFile = File(...)):
    if not file.filename:
        return error(400, "文件名不能为空")

    ext = file.filename.rsplit(".", 1)[-1].lower() if "." in file.filename else ""
    if ext not in ("xlsx", "xls", "csv"):
        return error(400, f"不支持的文件格式: .{ext}，仅支持 .xlsx .xls .csv")

    content = await file.read()
    max_bytes = MAX_FILE_SIZE_MB * 1024 * 1024
    if len(content) > max_bytes:
        return error(400, f"文件大小不能超过 {MAX_FILE_SIZE_MB}MB")

    try:
        svc = get_service()
        info = svc.ingest(file.filename, content)
        return success(data=info.model_dump(mode="json"))
    except Exception as e:
        return error(500, str(e))


@router.get("")
def list_datasets():
    svc = get_service()
    datasets = svc.list_all()
    return success(data=datasets)


@router.get("/{dataset_id}")
def get_dataset(dataset_id: str):
    svc = get_service()
    info = svc.get_info(dataset_id)
    if info is None:
        return error(404, "数据集不存在")
    return success(data=info)


@router.delete("/{dataset_id}")
def delete_dataset(dataset_id: str):
    svc = get_service()
    if svc.get_info(dataset_id) is None:
        return error(404, "数据集不存在")
    svc.delete(dataset_id)
    return success(message="已删除")


@router.get("/{dataset_id}/preview")
def preview(dataset_id: str, page: int = Query(1, ge=1), page_size: int = Query(50, ge=1, le=200)):
    svc = get_service()
    if svc.get_info(dataset_id) is None:
        return error(404, "数据集不存在")
    try:
        result = svc.preview(dataset_id, page, page_size)
        return success(data=result)
    except Exception as e:
        return error(500, str(e))
```

- [ ] **Step 2: Commit**

```bash
git add backend/app/api/
git commit -m "feat: add datasets API routes (upload, list, get, delete, preview)"
```

---

### Task 15: Backend API Routes — Cleaning, Query, Charts, Chat

**Files:**
- Create: `backend/app/api/cleaning.py`
- Create: `backend/app/api/query.py`
- Create: `backend/app/api/charts.py`
- Create: `backend/app/api/chat.py`

- [ ] **Step 1: Write cleaning API**

```python
from fastapi import APIRouter
from app.service.cleaning_service import CleaningService
from app.config import DUCKDB_DIR
from app.utils.response import success, error
from app.model.cleaning import CleanRequest
import os

router = APIRouter(prefix="/api/cleaning", tags=["cleaning"])

@router.post("/{dataset_id}/scan")
def scan(dataset_id: str):
    db_path = os.path.join(DUCKDB_DIR, f"{dataset_id}.duckdb")
    if not os.path.exists(db_path):
        return error(404, "数据集不存在")
    svc = CleaningService(db_path=db_path, table_name="data")
    issues = svc.scan()
    summary_parts = []
    for issue in issues:
        col_info = f"列'{issue.column}'" if issue.column else ""
        summary_parts.append(f"{issue.count}条{issue.type}")
    summary = "发现 " + "、".join(summary_parts) if summary_parts else "未发现问题"
    return success(data={"issues": [i.model_dump() for i in issues], "summary": summary})


@router.post("/{dataset_id}/execute")
def execute_cleaning(dataset_id: str, req: CleanRequest):
    db_path = os.path.join(DUCKDB_DIR, f"{dataset_id}.duckdb")
    if not os.path.exists(db_path):
        return error(404, "数据集不存在")
    svc = CleaningService(db_path=db_path, table_name="data")
    try:
        result = svc.execute(req.actions, req.fill_strategy)
        return success(data=result.model_dump())
    except Exception as e:
        return error(500, str(e))


@router.post("/{dataset_id}/undo")
def undo(dataset_id: str):
    db_path = os.path.join(DUCKDB_DIR, f"{dataset_id}.duckdb")
    if not os.path.exists(db_path):
        return error(404, "数据集不存在")
    svc = CleaningService(db_path=db_path, table_name="data")
    ok = svc.undo()
    if ok:
        return success(message="已撤销")
    return error(400, "没有可撤销的操作")
```

- [ ] **Step 2: Write query API**

```python
from fastapi import APIRouter
from app.service.query_service import QueryService
from app.config import DUCKDB_DIR
from app.utils.response import success, error
from app.model.query import QueryRequest

router = APIRouter(prefix="/api/query", tags=["query"])

@router.post("/execute")
def execute_query(req: QueryRequest):
    svc = QueryService(duckdb_dir=DUCKDB_DIR)
    try:
        result = svc.execute(
            dataset_id=req.dataset_id,
            sql=req.sql,
            natural_language=req.natural_language,
        )
        return success(data=result.model_dump())
    except FileNotFoundError:
        return error(404, "数据集不存在")
    except ValueError as e:
        return error(400, str(e))
    except Exception as e:
        return error(500, str(e))
```

- [ ] **Step 3: Write chart API**

```python
from fastapi import APIRouter
from app.service.chart_service import ChartService
from app.config import DUCKDB_DIR
from app.utils.response import success, error
from app.model.chart import ChartRecommendRequest
import os

router = APIRouter(prefix="/api/charts", tags=["charts"])

@router.post("/recommend")
async def recommend(req: ChartRecommendRequest):
    db_path = os.path.join(DUCKDB_DIR, f"{req.dataset_id}.duckdb")
    if not os.path.exists(db_path):
        return error(404, "数据集不存在")
    svc = ChartService(duckdb_dir=DUCKDB_DIR)
    try:
        charts = await svc.recommend(req.dataset_id, req.hint)
        return success(data={"charts": [c.model_dump() for c in charts]})
    except Exception as e:
        return error(500, str(e))
```

- [ ] **Step 4: Write chat API**

```python
from fastapi import APIRouter
from app.service.chat_service import ChatService
from app.config import DUCKDB_DIR
from app.utils.response import success, error
from app.model.chat import ChatRequest
import os

router = APIRouter(prefix="/api/chat", tags=["chat"])

@router.post("")
async def chat(req: ChatRequest):
    db_path = os.path.join(DUCKDB_DIR, f"{req.dataset_id}.duckdb")
    if not os.path.exists(db_path):
        return error(404, "数据集不存在")
    svc = ChatService(duckdb_dir=DUCKDB_DIR)
    try:
        result = await svc.chat(req.dataset_id, req.message, req.history)
        return success(data=result.model_dump())
    except Exception as e:
        return error(500, str(e))
```

- [ ] **Step 5: Commit**

```bash
git add backend/app/api/cleaning.py backend/app/api/query.py backend/app/api/charts.py backend/app/api/chat.py
git commit -m "feat: add cleaning, query, chart, and chat API routes"
```

---

### Task 16: FastAPI Main Entry & Router

**Files:**
- Create: `backend/app/api/router.py`
- Create: `backend/app/main.py`
- Create: `backend/.env.example`

- [ ] **Step 1: Write api/router.py**

```python
from fastapi import APIRouter
from app.api.datasets import router as datasets_router
from app.api.cleaning import router as cleaning_router
from app.api.query import router as query_router
from app.api.charts import router as charts_router
from app.api.chat import router as chat_router

api_router = APIRouter()
api_router.include_router(datasets_router)
api_router.include_router(cleaning_router)
api_router.include_router(query_router)
api_router.include_router(charts_router)
api_router.include_router(chat_router)
```

- [ ] **Step 2: Write main.py**

```python
import os
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.api.router import api_router
from app.data.file_store import FileStore
from app.config import UPLOAD_DIR, DUCKDB_DIR, DATA_RETENTION_DAYS

app = FastAPI(title="DataPilot API", version="0.1.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(api_router)


@app.on_event("startup")
async def startup():
    os.makedirs(UPLOAD_DIR, exist_ok=True)
    os.makedirs(DUCKDB_DIR, exist_ok=True)
    file_store = FileStore(UPLOAD_DIR)
    file_store.cleanup(DATA_RETENTION_DAYS)


@app.get("/api/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 3: Write .env.example**

```
DEEPSEEK_API_KEY=sk-your-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com
UPLOAD_DIR=uploads
DUCKDB_DIR=duckdb_data
MAX_FILE_SIZE_MB=50
DATA_RETENTION_DAYS=7
```

- [ ] **Step 4: Test that the server starts**

```bash
cd backend
cp .env.example .env
python -m uvicorn app.main:app --reload --port 8000
```
Expected: Server starts, `GET http://localhost:8000/api/health` returns `{"status":"ok"}`

- [ ] **Step 5: Test upload endpoint**

```bash
echo "name,age\nAlice,30\nBob,25" > /tmp/test.csv
curl -X POST http://localhost:8000/api/datasets/upload -F "file=@/tmp/test.csv"
```
Expected: JSON response with dataset info

- [ ] **Step 6: Commit**

```bash
git add backend/app/api/router.py backend/app/main.py backend/.env.example
git commit -m "feat: add FastAPI main entry with CORS and health check"
```

---

### Task 17: Frontend — Axios Client & Router

**Files:**
- Create: `frontend/src/api/client.ts`
- Create: `frontend/src/api/datasets.ts`
- Create: `frontend/src/api/cleaning.ts`
- Create: `frontend/src/api/query.ts`
- Create: `frontend/src/api/charts.ts`
- Create: `frontend/src/api/chat.ts`
- Create: `frontend/src/router/index.ts`
- Modify: `frontend/src/main.ts`

- [ ] **Step 1: Write api/client.ts**

```typescript
import axios from 'axios'
import { ElMessage } from 'element-plus'

const client = axios.create({
  baseURL: 'http://localhost:8000/api',
  timeout: 30000,
})

client.interceptors.response.use(
  (res) => {
    const data = res.data
    if (data.code !== 0) {
      ElMessage.error(data.message || '请求失败')
      return Promise.reject(new Error(data.message))
    }
    return data.data
  },
  (err) => {
    ElMessage.error('网络异常，请稍后重试')
    return Promise.reject(err)
  }
)

export default client
```

- [ ] **Step 2: Write api/datasets.ts**

```typescript
import client from './client'

export function uploadDataset(file: File) {
  const form = new FormData()
  form.append('file', file)
  return client.post('/datasets/upload', form, {
    headers: { 'Content-Type': 'multipart/form-data' },
  })
}

export function getDatasets() {
  return client.get('/datasets')
}

export function getDataset(id: string) {
  return client.get(`/datasets/${id}`)
}

export function deleteDataset(id: string) {
  return client.delete(`/datasets/${id}`)
}

export function previewDataset(id: string, page: number = 1, pageSize: number = 50) {
  return client.get(`/datasets/${id}/preview`, { params: { page, page_size: pageSize } })
}
```

- [ ] **Step 3: Write api/cleaning.ts**

```typescript
import client from './client'

export function scanIssues(datasetId: string) {
  return client.post(`/cleaning/${datasetId}/scan`)
}

export function executeCleaning(datasetId: string, actions: string[], fillStrategy: string = 'mean') {
  return client.post(`/cleaning/${datasetId}/execute`, {
    dataset_id: datasetId,
    actions,
    fill_strategy: fillStrategy,
  })
}

export function undoCleaning(datasetId: string) {
  return client.post(`/cleaning/${datasetId}/undo`)
}
```

- [ ] **Step 4: Write api/query.ts**

```typescript
import client from './client'

export function executeQuery(datasetId: string, sql?: string, naturalLanguage?: string) {
  return client.post('/query/execute', {
    dataset_id: datasetId,
    sql: sql || null,
    natural_language: naturalLanguage || null,
  })
}
```

- [ ] **Step 5: Write api/charts.ts**

```typescript
import client from './client'

export function recommendCharts(datasetId: string, hint?: string) {
  return client.post('/charts/recommend', {
    dataset_id: datasetId,
    hint: hint || null,
  })
}
```

- [ ] **Step 6: Write api/chat.ts**

```typescript
import client from './client'

export function sendMessage(datasetId: string, message: string, history?: { role: string; content: string }[]) {
  return client.post('/chat', {
    dataset_id: datasetId,
    message,
    history: history || null,
  })
}
```

- [ ] **Step 7: Write router/index.ts**

```typescript
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', name: 'Dashboard', component: () => import('../views/Dashboard.vue') },
    { path: '/datasets/:id', name: 'DatasetDetail', component: () => import('../views/DatasetDetail.vue') },
    { path: '/upload', name: 'Upload', component: () => import('../views/Upload.vue') },
    { path: '/history', name: 'History', component: () => import('../views/History.vue') },
  ],
})

export default router
```

- [ ] **Step 8: Update main.ts**

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(createPinia())
app.use(router)
app.use(ElementPlus)
app.mount('#app')
```

- [ ] **Step 9: Commit**

```bash
git add frontend/src/api/ frontend/src/router/ frontend/src/main.ts
git commit -m "feat: add frontend API layer and router"
```

---

### Task 18: Frontend — Pinia Store & Utils

**Files:**
- Create: `frontend/src/stores/useDataset.ts`
- Create: `frontend/src/utils/format.ts`

- [ ] **Step 1: Write stores/useDataset.ts**

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { getDataset } from '../api/datasets'

export interface ColumnInfo {
  name: string
  dtype: string
  null_count: number
  sample_values: any[]
}

export interface DatasetInfo {
  id: string
  name: string
  rows: number
  columns: number
  fields: ColumnInfo[]
  created_at: string
}

export const useDatasetStore = defineStore('dataset', () => {
  const current = ref<DatasetInfo | null>(null)
  const loading = ref(false)

  async function load(id: string) {
    loading.value = true
    try {
      current.value = await getDataset(id) as DatasetInfo
    } finally {
      loading.value = false
    }
  }

  function clear() {
    current.value = null
  }

  return { current, loading, load, clear }
})
```

- [ ] **Step 2: Write utils/format.ts**

```typescript
export function formatNumber(n: number | string): string {
  if (typeof n === 'string') n = parseFloat(n)
  if (isNaN(n)) return String(n)
  return n.toLocaleString('zh-CN')
}

export function formatDate(d: string): string {
  if (!d) return ''
  const date = new Date(d)
  if (isNaN(date.getTime())) return d
  return date.toLocaleDateString('zh-CN')
}

export function truncateText(text: string, max: number = 50): string {
  if (!text) return ''
  if (text.length <= max) return text
  return text.slice(0, max) + '...'
}
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/stores/ frontend/src/utils/
git commit -m "feat: add Pinia store and format utilities"
```

---

### Task 19: Frontend — Shared Components

**Files:**
- Create: `frontend/src/components/DataTable.vue`
- Create: `frontend/src/components/ChartPanel.vue`
- Create: `frontend/src/components/DatasetSidebar.vue`
- Create: `frontend/src/components/AiLoading.vue`
- Create: `frontend/src/App.vue`

- [ ] **Step 1: Write DataTable.vue**

```vue
<template>
  <div class="data-table-wrapper">
    <el-table
      :data="rows"
      stripe
      border
      v-loading="loading"
      :max-height="maxHeight"
      style="width: 100%"
    >
      <el-table-column
        v-for="col in columns"
        :key="col"
        :prop="col"
        :label="col"
        :min-width="120"
        show-overflow-tooltip
      />
    </el-table>
    <div class="pagination-bar" v-if="total > 0">
      <el-pagination
        v-model:current-page="currentPage"
        :page-size="pageSize"
        :total="total"
        layout="total, prev, pager, next"
        @current-change="$emit('page-change', $event)"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'

const props = withDefaults(defineProps<{
  columns: string[]
  rows: Record<string, any>[]
  total?: number
  page?: number
  pageSize?: number
  loading?: boolean
  maxHeight?: string
}>(), {
  total: 0,
  page: 1,
  pageSize: 50,
  loading: false,
  maxHeight: '500',
})

defineEmits<{ 'page-change': [page: number] }>()

const currentPage = computed(() => props.page)
</script>

<style scoped>
.data-table-wrapper { width: 100%; }
.pagination-bar { display: flex; justify-content: flex-end; margin-top: 12px; }
</style>
```

- [ ] **Step 2: Write ChartPanel.vue**

```vue
<template>
  <div class="chart-panel">
    <div v-if="!echartsOption" class="chart-placeholder">
      <el-empty description="点击「推荐图表」生成可视化" />
    </div>
    <div v-else ref="chartRef" class="chart-container"></div>
  </div>
</template>

<script setup lang="ts">
import { ref, watch, onMounted, nextTick } from 'vue'
import * as echarts from 'echarts'

const props = defineProps<{ echartsOption?: Record<string, any> | null }>()
const chartRef = ref<HTMLElement | null>(null)
let chartInstance: echarts.ECharts | null = null

function renderChart() {
  if (!chartRef.value || !props.echartsOption) return
  if (!chartInstance) {
    chartInstance = echarts.init(chartRef.value)
  }
  chartInstance.setOption(props.echartsOption, true)
}

watch(() => props.echartsOption, async (opt) => {
  if (opt) {
    await nextTick()
    renderChart()
  }
}, { deep: true })

onMounted(() => {
  if (props.echartsOption) renderChart()
  window.addEventListener('resize', () => chartInstance?.resize())
})
</script>

<style scoped>
.chart-panel { width: 100%; min-height: 350px; }
.chart-container { width: 100%; height: 350px; }
.chart-placeholder { display: flex; align-items: center; justify-content: center; min-height: 350px; }
</style>
```

- [ ] **Step 3: Write DatasetSidebar.vue**

```vue
<template>
  <div class="sidebar">
    <h4>表结构</h4>
    <el-tag v-for="field in fields" :key="field.name" class="field-tag" :type="tagType(field.dtype)">
      {{ field.name }}
      <span class="dtype">{{ field.dtype }}</span>
    </el-tag>
  </div>
</template>

<script setup lang="ts">
defineProps<{ fields: { name: string; dtype: string }[] }>()

function tagType(dtype: string): string {
  const t = dtype.toLowerCase()
  if (t.includes('int') || t.includes('float') || t.includes('double')) return 'warning'
  if (t.includes('date') || t.includes('time')) return 'success'
  return 'info'
}
</script>

<style scoped>
.sidebar { padding: 12px; }
.sidebar h4 { margin: 0 0 8px; font-size: 14px; color: #606266; }
.field-tag { margin: 4px 4px 4px 0; display: inline-flex; align-items: center; }
.dtype { font-size: 11px; opacity: 0.7; margin-left: 4px; }
</style>
```

- [ ] **Step 4: Write AiLoading.vue**

```vue
<template>
  <div class="ai-loading" v-if="visible">
    <div class="dot-pulse"></div>
    <span>{{ text }}</span>
  </div>
</template>

<script setup lang="ts">
withDefaults(defineProps<{ visible: boolean; text?: string }>(), { text: 'AI 思考中...' })
</script>

<style scoped>
.ai-loading { display: flex; align-items: center; gap: 8px; padding: 8px 16px; color: #909399; }
.dot-pulse { width: 8px; height: 8px; border-radius: 50%; background: #409eff; animation: pulse 1s ease-in-out infinite; }
@keyframes pulse { 0%, 100% { opacity: 0.3; transform: scale(0.8); } 50% { opacity: 1; transform: scale(1.2); } }
</style>
```

- [ ] **Step 5: Write App.vue**

```vue
<template>
  <el-container class="app-container">
    <el-header class="app-header">
      <router-link to="/" class="logo">DataPilot</router-link>
      <el-menu mode="horizontal" :default-active="route.path" router>
        <el-menu-item index="/">首页</el-menu-item>
        <el-menu-item index="/upload">上传数据</el-menu-item>
        <el-menu-item index="/history">历史记录</el-menu-item>
      </el-menu>
    </el-header>
    <el-main>
      <router-view />
    </el-main>
  </el-container>
</template>

<script setup lang="ts">
import { useRoute } from 'vue-router'
const route = useRoute()
</script>

<style>
body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; }
.app-container { min-height: 100vh; }
.app-header { display: flex; align-items: center; border-bottom: 1px solid #e4e7ed; }
.logo { font-size: 20px; font-weight: 700; color: #409eff; text-decoration: none; margin-right: 24px; }
</style>
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/ frontend/src/App.vue
git commit -m "feat: add shared components and app shell"
```

---

### Task 20: Frontend — Dashboard & Upload Views

**Files:**
- Create: `frontend/src/views/Dashboard.vue`
- Create: `frontend/src/views/Upload.vue`
- Create: `frontend/src/views/History.vue`

- [ ] **Step 1: Write Dashboard.vue**

```vue
<template>
  <div class="dashboard">
    <h2>我的数据集</h2>
    <div v-if="datasets.length === 0" class="empty-state">
      <el-empty description="暂无数据，去上传吧">
        <el-button type="primary" @click="$router.push('/upload')">上传数据</el-button>
      </el-empty>
    </div>
    <el-row :gutter="16" v-else>
      <el-col :span="8" v-for="ds in datasets" :key="ds.id" style="margin-bottom: 16px">
        <el-card shadow="hover" @click="$router.push(`/datasets/${ds.id}`)" class="ds-card">
          <h3>{{ ds.name }}</h3>
          <p>{{ ds.rows }} 行 · {{ ds.columns }} 列</p>
          <p class="time">{{ formatDate(ds.created_at) }}</p>
          <el-button type="danger" size="small" link @click.stop="handleDelete(ds.id)">删除</el-button>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { getDatasets, deleteDataset } from '../api/datasets'
import { formatDate } from '../utils/format'
import { ElMessageBox } from 'element-plus'

const datasets = ref<any[]>([])

onMounted(async () => {
  datasets.value = await getDatasets() as any[]
})

async function handleDelete(id: string) {
  await ElMessageBox.confirm('确定删除这个数据集吗？', '确认', { type: 'warning' })
  await deleteDataset(id)
  datasets.value = datasets.value.filter(d => d.id !== id)
}
</script>

<style scoped>
.dashboard h2 { margin-bottom: 16px; }
.ds-card { cursor: pointer; }
.ds-card h3 { margin: 0 0 8px; }
.ds-card p { margin: 4px 0; color: #909399; }
.empty-state { margin-top: 80px; }
</style>
```

- [ ] **Step 2: Write Upload.vue**

```vue
<template>
  <div class="upload-page">
    <h2>上传数据</h2>
    <el-upload
      drag
      :auto-upload="false"
      :on-change="handleChange"
      :limit="1"
      accept=".csv,.xlsx,.xls"
    >
      <el-icon class="el-icon--upload"><UploadFilled /></el-icon>
      <div class="el-upload__text">拖拽文件到此处，或点击上传</div>
      <template #tip>
        <div class="el-upload__tip">支持 .csv .xlsx .xls 格式，最大 50MB</div>
      </template>
    </el-upload>
    <div v-if="uploading" style="margin-top: 16px">
      <el-progress :percentage="100" :indeterminate="true" />
      <p>正在解析数据...</p>
    </div>
    <div v-if="result" style="margin-top: 16px">
      <el-alert type="success" :title="`上传成功：${result.name}`" :closable="false" />
      <p>{{ result.rows }} 行 · {{ result.columns }} 列</p>
      <el-button type="primary" @click="$router.push(`/datasets/${result.id}`)">查看数据</el-button>
      <el-button @click="$router.push('/upload')">继续上传</el-button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { UploadFilled } from '@element-plus/icons-vue'
import { uploadDataset } from '../api/datasets'
import { ElMessage } from 'element-plus'

const uploading = ref(false)
const result = ref<any>(null)

async function handleChange(file: any) {
  uploading.value = true
  result.value = null
  try {
    result.value = await uploadDataset(file.raw)
    ElMessage.success('上传成功')
  } catch (e: any) {
    ElMessage.error(e.message || '上传失败')
  } finally {
    uploading.value = false
  }
}
</script>

<style scoped>
.upload-page { max-width: 600px; margin: 0 auto; }
.upload-page h2 { margin-bottom: 16px; }
</style>
```

- [ ] **Step 3: Write History.vue** (placeholder)

```vue
<template>
  <div class="history-page">
    <h2>操作历史</h2>
    <el-empty description="Phase 2 即将上线" />
  </div>
</template>
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/views/
git commit -m "feat: add dashboard, upload, and history views"
```

---

### Task 21: Frontend — DatasetDetail View (Core Page)

**Files:**
- Create: `frontend/src/views/DatasetDetail.vue`

- [ ] **Step 1: Write DatasetDetail.vue**

```vue
<template>
  <div class="detail-page" v-loading="store.loading">
    <el-page-header @back="$router.push('/')">
      <template #content>
        <h2>{{ store.current?.name }}</h2>
      </template>
      <template #extra>
        <span>{{ store.current?.rows }} 行 · {{ store.current?.columns }} 列</span>
      </template>
    </el-page-header>

    <div class="detail-body">
      <el-tabs v-model="activeTab" type="border-card">
        <!-- Tab 1: 数据预览 -->
        <el-tab-pane label="数据预览" name="preview">
          <DataTable
            :columns="previewColumns"
            :rows="previewRows"
            :total="previewTotal"
            :page="previewPage"
            :pageSize="50"
            :loading="previewLoading"
            @page-change="loadPreview"
          />
        </el-tab-pane>

        <!-- Tab 2: 数据清洗 -->
        <el-tab-pane label="数据清洗" name="cleaning">
          <div class="cleaning-panel">
            <el-button type="primary" @click="scanIssues" :loading="scanning">扫描问题</el-button>
            <div v-if="issues.length > 0" style="margin-top: 16px">
              <h4>发现 {{ issues.length }} 类问题：</h4>
              <el-checkbox-group v-model="selectedActions">
                <div v-for="issue in issues" :key="issue.type" class="issue-item">
                  <el-checkbox :value="issue.type">
                    <el-tag :type="issue.severity === 'high' ? 'danger' : issue.severity === 'medium' ? 'warning' : 'info'">
                      {{ issue.type }}
                    </el-tag>
                    {{ issue.column ? `列"${issue.column}"` : '' }}
                    {{ issue.count }} 条
                    <span class="severity">[{{ issue.severity }}]</span>
                  </el-checkbox>
                </div>
              </el-checkbox-group>
              <div style="margin-top: 12px">
                <el-select v-model="fillStrategy" placeholder="空值填充策略" style="width: 160px; margin-right: 8px">
                  <el-option label="平均值" value="mean" />
                  <el-option label="中位数" value="median" />
                  <el-option label="删除行" value="drop" />
                </el-select>
                <el-button type="success" @click="executeClean" :disabled="selectedActions.length === 0">
                  执行清洗
                </el-button>
                <el-button @click="undoClean" v-if="canUndo">撤销</el-button>
              </div>
            </div>
            <div v-if="cleanResult" style="margin-top: 16px">
              <el-alert type="success" :closable="false">
                <p v-for="c in cleanResult.changes" :key="c">{{ c }}</p>
                <p>{{ cleanResult.rows_before }} → {{ cleanResult.rows_after }} 行</p>
              </el-alert>
            </div>
          </div>
        </el-tab-pane>

        <!-- Tab 3: 图表中心 -->
        <el-tab-pane label="图表中心" name="charts">
          <div class="chart-panel">
            <el-button type="primary" @click="loadCharts" :loading="chartLoading">
              AI 推荐图表
            </el-button>
            <div v-if="charts.length > 0" style="margin-top: 16px">
              <div v-for="(chart, idx) in charts" :key="idx" class="chart-item">
                <h4>{{ chart.title }} <el-tag size="small">{{ chart.chart_type }}</el-tag></h4>
                <p class="chart-reason">{{ chart.reason }}</p>
                <ChartPanel :echarts-option="chart.echarts_option" />
              </div>
            </div>
          </div>
        </el-tab-pane>

        <!-- Tab 4: SQL 查询 -->
        <el-tab-pane label="SQL 查询" name="query">
          <div class="query-panel">
            <div class="query-input-area">
              <el-radio-group v-model="queryMode" size="small" style="margin-bottom: 8px">
                <el-radio-button value="nl">自然语言</el-radio-button>
                <el-radio-button value="sql">SQL</el-radio-button>
              </el-radio-group>
              <el-input
                v-if="queryMode === 'nl'"
                v-model="nlInput"
                placeholder="例如：统计每月销售额"
                @keyup.enter="runQuery"
              />
              <el-input
                v-else
                v-model="sqlInput"
                type="textarea"
                :rows="3"
                placeholder="SELECT * FROM data WHERE ..."
              />
              <el-button type="primary" @click="runQuery" :loading="queryLoading" style="margin-top: 8px">
                执行
              </el-button>
            </div>
            <div v-if="queryExecutedSql" class="executed-sql">
              <el-tag>执行 SQL:</el-tag> <code>{{ queryExecutedSql }}</code>
            </div>
            <div v-if="queryExplanation" class="ai-explain">
              <el-alert type="info" :closable="false">{{ queryExplanation }}</el-alert>
            </div>
            <DataTable
              v-if="queryResult"
              :columns="queryColumns"
              :rows="queryRows"
              style="margin-top: 16px"
            />
          </div>
        </el-tab-pane>

        <!-- Tab 5: AI 对话 -->
        <el-tab-pane label="AI 对话" name="chat">
          <div class="chat-panel">
            <div ref="chatList" class="chat-messages">
              <div v-for="(msg, idx) in chatHistory" :key="idx" :class="['chat-msg', msg.role]">
                <strong>{{ msg.role === 'user' ? '你' : 'AI' }}:</strong>
                <div class="msg-content" v-html="formatChatContent(msg.content)"></div>
              </div>
              <AiLoading :visible="chatLoading" />
            </div>
            <div class="chat-input">
              <el-input v-model="chatInput" placeholder="输入问题..." @keyup.enter="sendChat" />
              <el-button type="primary" @click="sendChat" :loading="chatLoading">发送</el-button>
            </div>
          </div>
        </el-tab-pane>
      </el-tabs>

      <!-- Sidebar (visible on preview, query, chart tabs) -->
      <div class="sidebar-panel" v-if="store.current?.fields && activeTab !== 'chat' && activeTab !== 'cleaning'">
        <DatasetSidebar :fields="store.current.fields" />
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, watch, onMounted, nextTick } from 'vue'
import { useRoute } from 'vue-router'
import { useDatasetStore } from '../stores/useDataset'
import { previewDataset } from '../api/datasets'
import { scanIssues as apiScan, executeCleaning, undoCleaning } from '../api/cleaning'
import { executeQuery } from '../api/query'
import { recommendCharts } from '../api/charts'
import { sendMessage } from '../api/chat'
import DataTable from '../components/DataTable.vue'
import ChartPanel from '../components/ChartPanel.vue'
import DatasetSidebar from '../components/DatasetSidebar.vue'
import AiLoading from '../components/AiLoading.vue'

const route = useRoute()
const store = useDatasetStore()
const activeTab = ref('preview')

// Preview state
const previewColumns = ref<string[]>([])
const previewRows = ref<any[]>([])
const previewTotal = ref(0)
const previewPage = ref(1)
const previewLoading = ref(false)

// Cleaning state
const issues = ref<any[]>([])
const selectedActions = ref<string[]>([])
const fillStrategy = ref('mean')
const scanning = ref(false)
const cleanResult = ref<any>(null)
const canUndo = ref(false)

// Chart state
const charts = ref<any[]>([])
const chartLoading = ref(false)

// Query state
const queryMode = ref('nl')
const nlInput = ref('')
const sqlInput = ref('')
const queryLoading = ref(false)
const queryResult = ref(false)
const queryColumns = ref<string[]>([])
const queryRows = ref<any[]>([])
const queryExecutedSql = ref('')
const queryExplanation = ref('')

// Chat state
const chatHistory = ref<{ role: string; content: string }[]>([])
const chatInput = ref('')
const chatLoading = ref(false)
const chatList = ref<HTMLElement | null>(null)

onMounted(async () => {
  const id = route.params.id as string
  await store.load(id)
  if (store.current) loadPreview(1)
})

async function loadPreview(page: number) {
  previewLoading.value = true
  previewPage.value = page
  try {
    const data: any = await previewDataset(route.params.id as string, page, 50)
    previewColumns.value = data.columns
    previewRows.value = data.rows
    previewTotal.value = data.total_rows
  } finally {
    previewLoading.value = false
  }
}

async function scanIssues() {
  scanning.value = true
  try {
    const data: any = await apiScan(route.params.id as string)
    issues.value = data.issues
    selectedActions.value = data.issues.map((i: any) => i.type)
  } finally {
    scanning.value = false
  }
}

async function executeClean() {
  const data: any = await executeCleaning(route.params.id as string, selectedActions.value, fillStrategy.value)
  cleanResult.value = data
  canUndo.value = data.can_undo
  await store.load(route.params.id as string)
}

async function undoClean() {
  await undoCleaning(route.params.id as string)
  cleanResult.value = null
  canUndo.value = false
  await store.load(route.params.id as string)
}

async function loadCharts() {
  chartLoading.value = true
  try {
    const data: any = await recommendCharts(route.params.id as string)
    charts.value = data.charts
  } finally {
    chartLoading.value = false
  }
}

async function runQuery() {
  queryLoading.value = true
  try {
    const sql = queryMode.value === 'sql' ? sqlInput.value : undefined
    const nl = queryMode.value === 'nl' ? nlInput.value : undefined
    const data: any = await executeQuery(route.params.id as string, sql, nl)
    queryColumns.value = data.columns
    queryRows.value = data.rows
    queryExecutedSql.value = data.executed_sql
    queryExplanation.value = data.ai_explanation
    queryResult.value = true
  } finally {
    queryLoading.value = false
  }
}

async function sendChat() {
  if (!chatInput.value.trim()) return
  const msg = chatInput.value.trim()
  chatHistory.value.push({ role: 'user', content: msg })
  chatInput.value = ''
  chatLoading.value = true
  try {
    const data: any = await sendMessage(route.params.id as string, msg, chatHistory.value.slice(0, -1))
    chatHistory.value.push({ role: 'assistant', content: data.answer })
    await nextTick()
    if (chatList.value) chatList.value.scrollTop = chatList.value.scrollHeight
  } finally {
    chatLoading.value = false
  }
}

function formatChatContent(text: string): string {
  return text.replace(/```sql\n?([\s\S]*?)```/g, '<pre><code>$1</code></pre>').replace(/\n/g, '<br>')
}
</script>

<style scoped>
.detail-page { padding: 0; }
.detail-body { display: flex; gap: 16px; margin-top: 16px; }
.detail-body > .el-tabs { flex: 1; min-width: 0; }
.sidebar-panel { width: 220px; flex-shrink: 0; }

.cleaning-panel, .chart-panel, .query-panel { padding: 8px 0; }
.issue-item { margin: 8px 0; }
.severity { color: #909399; font-size: 12px; }
.executed-sql { margin: 8px 0; }
.executed-sql code { background: #f5f7fa; padding: 2px 6px; border-radius: 4px; }
.ai-explain { margin: 8px 0; }
.chart-item { margin-bottom: 24px; }
.chart-reason { color: #909399; font-size: 13px; margin: 4px 0 8px; }

.chat-panel { display: flex; flex-direction: column; height: 500px; }
.chat-messages { flex: 1; overflow-y: auto; padding: 12px; background: #f9fafb; border-radius: 8px; margin-bottom: 12px; }
.chat-msg { margin-bottom: 12px; }
.chat-msg.user strong { color: #409eff; }
.chat-msg.assistant strong { color: #67c23a; }
.msg-content { margin-top: 4px; white-space: pre-wrap; word-break: break-word; }
.msg-content pre { background: #e8e8e8; padding: 8px; border-radius: 4px; overflow-x: auto; }
.chat-input { display: flex; gap: 8px; }
.chat-input .el-input { flex: 1; }
</style>
```

- [ ] **Step 2: Test frontend starts**

```bash
cd frontend && npm run dev
```
Expected: Vite dev server starts on localhost:5173

- [ ] **Step 3: Commit**

```bash
git add frontend/src/views/DatasetDetail.vue
git commit -m "feat: add DatasetDetail view with 5 tabs (preview, clean, chart, query, chat)"
```

---

### Task 22: Integration Test

**Files:**
- Create: `backend/tests/test_api_integration.py`

- [ ] **Step 1: Write integration test**

```python
import pytest
from fastapi.testclient import TestClient
from app.main import app
import os
import shutil

client = TestClient(app)

TEST_UPLOAD = "backend/test_upload"
TEST_DUCKDB = "backend/test_duckdb"

@pytest.fixture(autouse=True)
def setup_dirs():
    os.makedirs(TEST_UPLOAD, exist_ok=True)
    os.makedirs(TEST_DUCKDB, exist_ok=True)
    yield
    shutil.rmtree(TEST_UPLOAD, ignore_errors=True)
    shutil.rmtree(TEST_DUCKDB, ignore_errors=True)

def _create_csv():
    import tempfile
    f = tempfile.NamedTemporaryFile(suffix=".csv", delete=False, mode="w", encoding="utf-8")
    f.write("name,age,city\nAlice,30,Beijing\nBob,25,Shanghai\nCharlie,35,Beijing\nAlice,30,Beijing\n")
    f.close()
    return f.name

def test_upload_and_preview():
    csv_path = _create_csv()
    with open(csv_path, "rb") as f:
        resp = client.post("/api/datasets/upload", files={"file": ("test.csv", f, "text/csv")})
    assert resp.status_code == 200
    data = resp.json()
    assert data["code"] == 0
    ds_id = data["data"]["id"]
    assert data["data"]["rows"] == 4

    # Preview
    resp2 = client.get(f"/api/datasets/{ds_id}/preview?page=1&page_size=2")
    assert resp2.status_code == 200
    preview = resp2.json()["data"]
    assert preview["total_rows"] == 4
    assert len(preview["rows"]) == 2

    os.unlink(csv_path)

def test_upload_invalid_format():
    resp = client.post("/api/datasets/upload", files={"file": ("test.txt", b"hello", "text/plain")})
    assert resp.status_code == 200
    assert resp.json()["code"] == 400

def test_cleaning_scan_and_execute():
    csv_path = _create_csv()
    with open(csv_path, "rb") as f:
        resp = client.post("/api/datasets/upload", files={"file": ("test.csv", f, "text/csv")})
    ds_id = resp.json()["data"]["id"]

    # Scan
    scan_resp = client.post(f"/api/cleaning/{ds_id}/scan")
    assert scan_resp.status_code == 200
    issues = scan_resp.json()["data"]["issues"]
    assert any(i["type"] == "duplicate" for i in issues)

    # Execute dedup
    clean_resp = client.post(f"/api/cleaning/{ds_id}/execute", json={
        "dataset_id": ds_id,
        "actions": ["deduplicate"],
        "fill_strategy": "mean",
    })
    assert clean_resp.status_code == 200
    result = clean_resp.json()["data"]
    assert result["rows_after"] < result["rows_before"]

    os.unlink(csv_path)

def test_query_execute():
    csv_path = _create_csv()
    with open(csv_path, "rb") as f:
        resp = client.post("/api/datasets/upload", files={"file": ("test.csv", f, "text/csv")})
    ds_id = resp.json()["data"]["id"]

    # SQL query
    resp2 = client.post("/api/query/execute", json={
        "dataset_id": ds_id,
        "sql": "SELECT city, COUNT(*) as cnt FROM data GROUP BY city",
    })
    assert resp2.status_code == 200
    result = resp2.json()["data"]
    assert result["row_count"] >= 1
    assert "city" in result["columns"]

    os.unlink(csv_path)
```

- [ ] **Step 2: Run integration tests**

```bash
cd backend && python -m pytest tests/test_api_integration.py -v
```
Expected: 4 PASS

- [ ] **Step 3: Run ALL backend tests**

```bash
cd backend && python -m pytest tests/ -v
```
Expected: All tests pass (non-AI tests)

- [ ] **Step 4: Commit**

```bash
git add backend/tests/test_api_integration.py
git commit -m "test: add integration tests for upload, cleaning, and query APIs"
```

---

### Task 23: Error Handling Polish

**Files:**
- Modify: `backend/app/main.py`

- [ ] **Step 1: Add global exception handler to main.py**

Add the following import and handler after the existing middleware setup in `backend/app/main.py`:

```python
from fastapi import Request
from fastapi.responses import JSONResponse
from app.utils.response import error

@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    return JSONResponse(
        status_code=500,
        content=error(500, f"服务器内部错误: {str(exc)}"),
    )
```

- [ ] **Step 2: Add .gitignore**

Create `backend/.gitignore`:
```
__pycache__/
*.pyc
.env
uploads/
duckdb_data/
*.duckdb
```

Create `frontend/.gitignore`:
```
node_modules/
dist/
```

- [ ] **Step 3: Commit**

```bash
git add backend/app/main.py backend/.gitignore frontend/.gitignore
git commit -m "fix: add global exception handler and .gitignore files"
```

---

## Self-Review Results

**Spec coverage:** Every spec section is mapped to tasks:
- Config & utils → Task 3
- File parser → Task 4
- DuckDB manager → Task 5
- File store → Task 6
- Dataset models + service → Tasks 7-8
- Cleaning models + service → Tasks 9-10
- DeepSeek client → Task 11
- Query service → Task 12
- Chart/Chat services → Task 13
- API routes → Tasks 14-16
- Frontend layers → Tasks 17-21
- Integration tests → Task 22
- Error handling → Task 23

**Placeholder scan:** No TBD, TODO, "add error handling later" found. All steps contain actual code.

**Type consistency:** Models match service signatures. API routes call services with correct parameter names. Frontend API calls match backend endpoint paths and parameter names.
